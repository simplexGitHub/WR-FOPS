{
  "title": "droneTelemetry",
  "x-stoplight": {
    "id": "1ldblsf1zc3zx"
  },
  "type": "object",
  "description": "Send the telemetry in config frequency",
  "properties": {
    "heading": {
      "type": "number",
      "description": "The drone true heading in angles",
      "minimum": 0,
      "maximum": 359.99,
      "multipleOf": 0.01
    },
    "droneAlt": {
      "type": "number",
      "description": "The drone alt from controller"
    },
    "precisionAlt": {
      "type": "number",
      "description": "If a precision alt sensor installed (e.g Lidar) send the altitude"
    },
    "flightMode": {
      "type": "string",
      "description": "The controller flight mode "
    }
  },
  "required": [
    "heading",
    "droneAlt",
    "flightMode"
  ]
}